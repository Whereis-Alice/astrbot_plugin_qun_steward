# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.1.0] - 2026-08-31

围绕「群聊还有哪些接口能用」做了一轮系统补齐：新增群情报榜单、AI 声聊、群文件整理、相册云端浏览，投票禁言改为贴表情计票，待审进群与协议端对账。

### 新增

- **投票禁言支持贴表情计票**：公告发出后机器人自己先贴好 👍 / 👎，群友点一下就算一票，取消表情即撤票，不用再刷指令。新增配置「计票方式」（`两者都行` / `贴表情` / `发指令`）与「赞同 / 反对表情 ID」（默认 76 / 77）。协议端不支持贴表情接口时自动退回指令计票并在公告里说明。
- **待审进群与协议端对账**：`待审进群` 会先调 `get_group_system_msg` 拉取协议端仍挂着的申请，本地没有的补录进队列，本地有而协议端已经没有的（别人在手机上处理过 / 申请过期）标记关闭，并在回复里说明补录与清理了多少条。同时读取「忽略的加群通知」避免重复入队。
- **群情报**（`features/insight.py`）：`群信息` 输出群等级、人数、加群方式、当前管理策略与 @全体剩余次数；`群荣誉` 输出龙王、群聊之火、群聊炽焰、冒尖小春笋、快乐源泉榜单卡片；`禁言列表` 走 `get_group_shut_list` 列出当前被禁言的人与剩余时间。
- **AI 声聊**（`features/voice.py`）：`声聊 文本` 让机器人用群 AI 音色把话念出来，`声聊音色` 列出本群可用音色，`声聊 2 文本` 临时换用第 2 个音色。音色列表按群缓存 30 分钟。
- **群文件补齐**：下载直链、移动、改名、容量统计（已用 / 总量 / 文件数 / 上限），以及「整理群文件」按规则把根目录散落的文件归入目录（可按类型 / 后缀 / 关键词 / 上传者规则）。
- **群相册云端化**：`查看群相册` 列相册与相册内图片序号，`删相册图 相册名 序号` 删除云端图片，随机图改为默认直接读云端相册（`cloud_random`），不再依赖本地备份目录；协议端不支持云端列图时自动退回本地。
- **删除群公告**：`删除群公告 序号` 走 `_del_group_notice`，上游只能发不能撤。
- **@全体成员先查剩余次数**：发之前调 `get_group_at_all_remain`，次数用完直接告知，不再白发一条被协议端吞掉的消息。
- **长列表合并转发**（`core/messaging.py`）：群友清理预览、审计日志、禁词表、相册列表等长回复改用合并转发发出，超长自动分节点，不再刷屏；协议端不支持时退回普通文本。

### 变更

- **批量踢人改用 `set_group_kick_members`**：一次请求提交全部成员，比逐个 `set_group_kick` 快得多、也更不容易触发风控；协议端不支持时自动退回逐个踢。
- **跨端调用统一收敛到 `core/protocol.py`**：同一能力声明多个候选动作名，逐个尝试直到成功；「该协议端不支持某动作」的结论按 (协议端, 动作) 缓存，不会每次都重试一遍。

### 修复

- **邀请入群类申请永远处理失败**（上游问题）：审批时固定传 `sub_type="add"`，而被邀请入群的申请需要 `sub_type="invite"`。现在先试 `add`，失败再试 `invite`。

### 其他

- `metadata.yaml` 与 README 的指令清单、帮助文本同步补齐所有新指令。
- 测试总数 309 → 360（新增进群对账归一化、声聊音色解析、群文件整理规则、相册云端缓存四组用例）。
## [v1.0.3] - 2026-08-31

小幅打磨上传群相册的回复体验。

### 变更

- **上传群相册成功后不再回复文字**：QQ 客户端上传成功时自己就会显示相册卡片，插件那句「已上传到相册【xxx】」纯属重复播报，现已改为静默。上传失败仍会说明原因；相册是插件顺手新建的时候（客户端不会提示）依然回一句「已新建相册【xxx】并上传成功。」

### 测试

- 用例数 307 → 309（新增两条：上传成功静默、新建相册时的提示文案）。

## [v1.0.2] - 2026-08-31

修复插件装不上、WebUI 打不开这两个致命问题，并重制面板皮肤。

### 修复

- **插件无法安装（致命）**：报错 `Conflicting modules: pilmoji -> module 'emoji.unicode_codes' has no attribute 'get_emoji_unicode_dict'`。根因是 requirements 里 pin 的 `pilmoji==2.0.4` + `emoji==1.7.0` 与 AstrBot 4.27+ 运行时的 `emoji>=2` 不兼容，而 AstrBot 会在加载插件前真实 import 依赖做预检，冲突即拒绝加载整个插件。现已彻底移除这两个依赖，emoji 绘制改为自研实现，依赖只剩 `aiosqlite` / `apscheduler` / `aiohttp` / `pillow`。
- **WebUI 报「未检测到 AstrBot 页面运行环境」**：面板脚本原来在首次执行时就快照 `window.AstrBotPluginPage`，而 AstrBot 注入 bridge SDK 的时机可能更晚，快照到 null 后所有请求都失败。改为惰性获取 + 最长 20 秒轮询等待，超时才显示带排查建议的诊断卡片。

### 新增

- **自研零依赖 emoji 渲染**（`features/album/emoji_text.py`）：按字符簇切分文本，普通文字与彩色 emoji 分别用对应字体绘制后拼接；彩色字体按「自定义路径 → 系统字体」查找，找不到时优雅降级。
- 新增配置项 `fonts.custom_emoji_font_path`，可手动指定彩色 emoji 字体（例如自备 `NotoColorEmoji.ttf`）。
- **WebUI 皮肤全面重制**：蓝紫渐变主题（与 logo 同色系）、极光背景、侧栏导航、毛玻璃顶栏、骨架屏、卡片式统计、自绘开关、窄屏胶囊导航，并尊重系统「减少动态效果」偏好。
- 新增 3 个防漂移测试，校验页面 id、CSS 类名与 app.js 实际用到的保持一致，以及页面只引用相对路径资源。
- README 增补「能上传原图吗」与「Emoji 渲染」两节，说明单图为原始字节直传、拼接长图会重编码，以及 ZWJ 组合与国旗字形的已知限制。

### 其他

- 测试总数 270 → 307。

## [v1.0.1] - 2026-08-31

按 llbot 与 SnowLuma 的官方接口清单校正群相册适配。

### 修复

- **llbot 上传失败被当成成功**：llbot 的 upload_group_album 即使全部失败也返回 status: ok / retcode: 0，失败信息在 data.fail_count / fail_indexes 里。现在会检查这些字段，失败时改用下一种图片参数形式重试，全部失败才如实报错。
- **llbot 图片参数顺序**：改为优先 file:// URI（官方示例形式），其次 base64，最后裸本地路径，少一次注定失败的往返。
- **SnowLuma 相册列表动作**：改为优先调用 get_qun_album_list（与 NapCat 对齐的那个），失败再退回 get_group_album_list；单个动作缺失或返回失败时会自动尝试下一个。

### 新增

- 配置里指定的默认相册不存在时，在支持新建相册的协议端（llbot）上会自动创建后再上传，提示语会说明已新建。
- README 增加「群相册的协议端差异」对照表，写明三端的动作名、参数差异与返回字段差异。
- 协议适配新增 25 项测试（三端动作选择、失败判定、图片参数顺序、自动建相册），测试总数 270。

## [v1.0.0] - 2026-08-30

首个正式版本。由 Zhalslar 的 astrbot_plugin_qqadmin（v3.4.0）与 astrbot_plugin_qun_album（v1.2.1）融合重写而来，指令名保持与上游一致，方便直接替换。

### 新增

- **群管 + 群相册合体**：两套功能共用一份权限模型、审计日志与数据层，不必再装两个插件。
- **操作审计**：所有管理动作写入 SQLite，支持按群 / 动作 / 操作者 / 关键词检索，可设置保留天数自动清理。群内用「操作日志」查看。
- **危险操作撤销**：踢人、拉黑、上管下管、全员禁言等留有时间窗口，群内发「撤销」即可回滚最近一次操作。
- **进群审批队列**：待审申请落库并带序号，重启不丢；支持「批准 <序号>」「驳回 <序号> <理由>」或直接引用申请通知处理。
- **默认模板 + 按群覆写**：全局配一次默认值，个别群只写差异项；未覆写的群永远跟随默认值变化。支持「群管配置」中文文本导入导出，以及「群管重置」回到跟随状态。
- **全新 WebUI 面板**：概览、分群配置、默认模板、权限矩阵、全局设置、违禁词、操作日志、待审进群、撤销栈、相册状态与运行时诊断；深浅色主题 + 中英文双语。
- **协议端自动探测**：识别 NapCat、LLOneBot（llbot）、SnowLuma，按各端能力对非标准接口降级调用，探测结果带缓存。
- **权限矩阵**：34 项操作可分别指定所需身份（超管 / 群主 / 管理员 / 高等级成员 / 成员），可按群覆写；「高等级成员」由可配置的群等级阈值判定。
- **违禁词增强**：内置词库开关、自定义词表增删（"+词" / "-词"，不带符号则整表覆盖）、整词与正则匹配、白名单与豁免身份、分级处理动作。
- **15 个 LLM 函数调用**：可让模型代为执行禁言、改名、头衔、公告、精华、群文件等操作，受同一套权限校验约束，并可全局关闭。
- **批量操作节流**：清理群友等批量动作带并发上限与总量上限，避免打爆协议端或触发风控。
- **中文操作日志与提示**：所有面向用户的文案统一中文，动作名带可读标签。

### 修复（相对上游）

- 权限键与实际指令错配，导致部分指令的权限设置不生效。
- 刷屏检测读取了错误的配置字段名，配置值形同无效。
- 纯文字群公告因缺少图片参数而发送失败。
- 配置重置会连同不该清空的项目一起删除。
- 投票禁言的状态字典只增不减，长期运行内存持续增长。
- 精华消息列表与群成员列表的排版在长昵称 / 缺字段时错行。
- 「我朋友说」历史消息扫描没有上限，大群会拉取过量消息。
- 宵禁任务从历史记录恢复时未传入管理器，失败后无法自愈。
- 协议端 client 尚未就绪时宵禁会抛 UnboundLocalError。
- 插件卸载时宵禁调度器未关停，残留后台线程。
- 群文件路径解析在协议端返回缺字段时崩溃。
- 权限等级解析把「成员」当作空值处理（0 被误判为假），默认权限被抬高到管理员。
- 下载失败、协议端接口缺失等异常缺少兜底，会把原始异常抛给用户。
- 另有十余处空值判断、编码、时区与提示文案问题。

### 变更

- 插件标识改为 astrbot_plugin_qun_steward（显示名「群务管家」），与上游插件互不冲突；数据库文件为 qun_steward.db。
- 代码分层重构：core/ 基础设施、features/ 业务功能、web/ 面板接口，main.py 只做编排；全量类型注解，附 245 项单元测试。
- 许可证为 GPLv3（继承 astrbot_plugin_qqadmin）。
